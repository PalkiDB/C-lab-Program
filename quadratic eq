//Quadratic equation
#include<stdio.h>
include<maths.h>
void main ()
{
//variable declaration
	float a, b, c, d, r1, r2, r part, i part ;
	printf("enter the coefficients:");
	scanf("%f %f %f," &a ,&b ,&c);
	if(a!=0)
	{
		d=(b*b)-(4*a*C);
		if (d==0)
	}
		{
	
			r1 = (-b)/(2*a);
			r2 = r1 ;
			printf("the roots are equal r1=%f and r2=%f,"r1r2);
		}
		else if (d>0)
		{
			r1 = -b+d/2*a
			r2 = -b-d/2*a
		}
		else
		{
			rpart= (-b)/(2*a)
			ipart= sqrt(-d)/(2*a)
		}
		printf("the roots are imaginary: %f+i%f", rpart, ipart);
		printf("the roots are imaginary: %f-i%f", rpart, ipart);
		}
	else
	{
	print("not a quadratic equation");
	}
